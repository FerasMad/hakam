# What the running app needs - no training, dataset or notebook tools.
# torch is installed first from the CPU wheel index (see Dockerfile); on a GPU host
# install the CUDA wheel instead.
numpy>=1.24
pandas>=2.0
transformers>=4.40
opencv-python-headless>=4.8
sentence-transformers>=3.0
openai>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0
huggingface_hub>=0.23
httpx>=0.27
